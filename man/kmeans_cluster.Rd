% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k_means.R
\name{kmeans_cluster}
\alias{kmeans_cluster}
\title{K-means clustering}
\usage{
kmeans_cluster(
  data,
  k,
  method = c("full", "minibatch"),
  kmeans_params = params_kmeans(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to cluster, of shape
samples x features. Will be coerced to a matrix.}

\item{k}{Integer. Number of clusters to create. Must be >= 2.}

\item{method}{Character. Clustering method. One of \code{"full"} (Lloyd's
algorithm) or \code{"minibatch"} (mini-batch k-means). Defaults to \code{"full"}.}

\item{kmeans_params}{Named list. K-means parameters, see \code{\link[=params_kmeans]{params_kmeans()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A named list with:
\describe{
\item{centroids}{Numeric matrix of shape k x features containing the
final cluster centroids.}
\item{assignments}{Integer vector of length samples with cluster
assignments (1-indexed).}
}
}
\description{
Performs k-means clustering on the input data. Supports both
full Lloyd's iterations (with SIMD/GEMM acceleration) and mini-batch k-means
for large data sets.
}
