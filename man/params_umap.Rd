% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_umap}
\alias{params_umap}
\title{Wrapper function to generate UMAP parameters}
\usage{
params_umap(
  local_connectivity = 1,
  bandwidth = 1e-05,
  mix_weight = 1,
  lr = 1,
  n_epochs = NULL,
  neg_sample_rate = 5L,
  gamma = 1,
  optimiser = c("adam_parallel", "sgd", "adam"),
  init = c("spectral", "pca", "random"),
  randomised = TRUE
)
}
\arguments{
\item{local_connectivity}{Numeric. Number of nearest neighbours assumed to be
at distance zero. Defaults to \code{1.0}.}

\item{bandwidth}{Numeric. Convergence tolerance for smooth kNN distance
binary search. Defaults to \code{1e-05}.}

\item{mix_weight}{Numeric. Balance between fuzzy union and directed graph
during symmetrisation. Defaults to \code{1.0}.}

\item{lr}{Numeric. Learning rate. Defaults to \code{1.0}.}

\item{n_epochs}{Integer or \code{NULL}. Number of optimisation epochs. If \code{NULL},
resolved downstream based on data size. Defaults to \code{NULL}.}

\item{neg_sample_rate}{Integer. Number of negative samples per positive
sample. Defaults to \code{5L}.}

\item{gamma}{Numeric. Repulsion strength. Defaults to \code{1.0}.}

\item{optimiser}{String. The optimiser. One of \code{c("adam_parallel", "sgd", "adam")}. Defaults to \code{"adam_parallel"}.}

\item{init}{String. Embedding initialisation method. One of \code{c("spectral", "pca", "random")}. Defaults to \code{"spectral"}.}

\item{randomised}{Boolean. Use randomised SVD for PCA initialisation.
Defaults to \code{TRUE}.}
}
\value{
A named list with the following elements:
\itemize{
\item local_connectivity - Numeric. Number of nearest neighbours assumed to
be at distance zero. Defaults to \code{1.0}.
\item bandwidth - Numeric. Convergence tolerance for smooth kNN distance
binary search. Defaults to \code{1e-05}.
\item mix_weight - Numeric. Balance between fuzzy union and directed graph
during symmetrisation. Defaults to \code{1.0}.
\item lr - Numeric. Learning rate. Defaults to \code{1.0}.
\item n_epochs - Integer or \code{NULL}. Number of optimisation epochs. If
\code{NULL}, resolved downstream based on data size. Defaults to \code{NULL}.
\item neg_sample_rate - Integer. Number of negative samples per positive
sample. Defaults to \code{5L}.
\item gamma - Numeric. Repulsion strength. Defaults to \code{1.0}.
\item optimiser - String. The optimiser. One of \code{c("adam_parallel", "sgd", "adam")}. Defaults to \code{"adam_parallel"}.
\item init - String. Embedding initialisation method. One of \code{c("spectral", "pca", "random")}. Defaults to \code{"spectral"}.
\item randomised - Boolean. Use randomised SVD for PCA initialisation.
Defaults to \code{TRUE}.
}
}
\description{
Wrapper function to generate UMAP parameters
}
