% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fa2.R
\name{forceatlas2_from_graph}
\alias{forceatlas2_from_graph}
\title{Rust-based ForceAtlas2 on a pre-computed graph}
\usage{
forceatlas2_from_graph(
  graph,
  init = NULL,
  fa2_params = params_fa2(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{graph}{An undirected \code{igraph} object without multi-edges. Use
\code{igraph::as_undirected(mode = "collapse")} or \code{igraph::simplify()} to get
there. If present, the \code{weight} edge attribute needs to be finite and
non-negative.}

\item{init}{Optional numerical matrix of dimensions \verb{vcount(graph) x 2}.
The initial layout, e.g. a UMAP of the same samples. If \code{NULL}, a random
layout is used. Defaults to \code{NULL}.}

\item{fa2_params}{Named list. ForceAtlas2 parameters, see \code{\link[=params_fa2]{params_fa2()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{use_high_precision}{Optional boolean. Gives fine-grained control over
\code{fp32} vs \code{fp64} usage.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions \verb{vcount(graph) x 2} containing
the ForceAtlas2 layout, rows in vertex order.
}
\description{
Lays out any undirected graph with ForceAtlas2, for example an
SNN graph or a graph from another package. Edge weights come from the
\code{weight} edge attribute; without one, every edge weighs \code{1}. Self-loops are
dropped. Only the optimisation parameters in \code{fa2_params} are used; the
graph and initialisation ones do not apply here.
}
\references{
Jacomy, et al., PLoS ONE, 2014
}
