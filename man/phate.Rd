% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phate.R
\name{phate}
\alias{phate}
\title{Rust-based PHATE}
\usage{
phate(
  data,
  knn = NULL,
  n_dim = 2L,
  k = 5L,
  knn_method = c("balltree", "hnsw", "annoy", "nndescent", "exhaustive"),
  nn_params = params_nn(),
  phate_params = params_phate(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to embed of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, PHATE will skip
the k-nearest neighbour graph generation and use this one instead. The \code{k}
argument must match the k used to generate this graph. Defaults to \code{NULL}.}

\item{n_dim}{Integer. Number of dimensions in the embedding space.
Currently only \code{2L} is supported. Defaults to \code{2L}.}

\item{k}{Integer. Number of nearest neighbours for graph construction.
Defaults to \code{5L}.}

\item{knn_method}{Character. Approximate nearest neighbour algorithm to use.
One of \code{"hnsw"}, \code{"annoy"}, \code{"nndescent"}, \code{"balltree"}, or
\code{"exhaustive"}. Defaults to \code{"balltree"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn]{params_nn()}}.}

\item{phate_params}{Named list. PHATE algorithm parameters, see
\code{\link[=params_phate]{params_phate()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions samples x n_dim containing
the PHATE embedding.
}
\description{
Performs PHATE dimensionality reduction on the input data.
This function provides a user-friendly interface with input validation
before calling the Rust implementation.
}
