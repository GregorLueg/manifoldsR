% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-prelude-generated.R
\name{apply_qtest_rules}
\alias{apply_qtest_rules}
\title{Apply qtest rules by name}
\usage{
apply_qtest_rules(x, rules, label, hint = NULL)
}
\arguments{
\item{x}{Named list of parameters to validate.}

\item{rules}{Named list mapping field name to a qtest pattern (or vector of
patterns passed to \code{qtest}).}

\item{label}{Short human-readable label used in the error message
(e.g. \code{"GSEA params"}).}

\item{hint}{Optional string appended to the error message to describe the
expected types/ranges. Defaults to \code{NULL} (no hint).}
}
\value{
\code{TRUE} if all checked fields pass, otherwise a string of the form
\code{"The element `<field>` in <label> is invalid. <hint>"}.
}
\description{
Validates the elements of a named list \code{x} against per-field
\code{\link[checkmate:qtest]{checkmate::qtest()}} patterns. Fields whose names are not in \code{rules} are
skipped. On failure, returns an error string naming the first offending
element and appending an optional \code{hint}.
}
\keyword{internal}
