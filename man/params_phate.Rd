% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_phate}
\alias{params_phate}
\title{Wrapper function to generate PHATE parameters}
\usage{
params_phate(
  decay = 40,
  bandwidth_scale = NULL,
  t_max = 100L,
  t_custom = NULL,
  gamma = 1,
  n_landmarks = 2048L,
  landmark_method = c("spectral", "random", "density"),
  n_svd = NULL,
  mds_method = c("sgd_dense", "classic"),
  graph_symmetry = c("additive", "multiplicative", "mnn", "none")
)
}
\arguments{
\item{decay}{Numeric. Alpha decay parameter controlling the kernel bandwidth.
Higher values create sharper transitions. Defaults to \code{40.0}.}

\item{bandwidth_scale}{Numeric or \code{NULL}. Scaling factor applied to the
kernel bandwidth. If \code{NULL}, the library selects a sensible default. Defaults
to \code{NULL}.}

\item{t_max}{Integer. Maximum diffusion time considered during automatic
selection via Von Neumann entropy knee point detection. Defaults to \code{100L}.}

\item{t_custom}{Integer or \code{NULL}. Fixed diffusion time. If set, overrides
automatic time selection. Defaults to \code{NULL}.}

\item{gamma}{Numeric. Informational distance parameter for the diffusion
potential. Defaults to \code{1.0}.}

\item{n_landmarks}{Integer or \code{NULL}. Number of landmarks for compressed
diffusion. If \code{NULL}, the full N x N diffusion operator is used. Defaults to
\code{2048L}.}

\item{landmark_method}{String. Method used to select landmarks. One of
\code{c("spectral", "random", "density")}. Defaults to \code{"spectral"}.}

\item{n_svd}{Integer or \code{NULL}. Number of SVD components used in landmark
construction. If \code{NULL}, the library selects a sensible default. Defaults to
\code{NULL}.}

\item{mds_method}{String. MDS algorithm used for the final embedding. One of
\code{c("sgd_dense", "classic")}. Defaults to \code{"sgd_dense"}.}

\item{graph_symmetry}{String. Method used to symmetrise the affinity graph.
One of \code{c("additive", "multiplicative", "mnn", "none")}. Defaults to
\code{"additive"}.}
}
\value{
A named list with the following elements:
\itemize{
\item decay - Numeric. Alpha decay parameter controlling the kernel
bandwidth. Higher values create sharper transitions. Defaults to \code{40.0}.
\item bandwidth_scale - Numeric or \code{NULL}. Scaling factor applied to the
kernel bandwidth. If \code{NULL}, the library selects a sensible default.
Defaults to \code{NULL}.
\item t_max - Integer. Maximum diffusion time considered during automatic
selection via Von Neumann entropy knee point detection. Defaults to \code{100L}.
\item t_custom - Integer or \code{NULL}. Fixed diffusion time. If set, overrides
automatic time selection. Defaults to \code{NULL}.
\item gamma - Numeric. Informational distance parameter for the diffusion
potential. Defaults to \code{1.0}.
\item n_landmarks - Integer or \code{NULL}. Number of landmarks for compressed
diffusion. If \code{NULL}, the full N x N diffusion operator is used. Defaults to
\code{2048L}.
\item landmark_method - String. Method used to select landmarks. One of
\code{c("spectral", "random", "density")}. Defaults to \code{"spectral"}.
\item n_svd - Integer or \code{NULL}. Number of SVD components used in landmark
construction. If \code{NULL}, the library selects a sensible default. Defaults to
\code{NULL}.
\item mds_method - String. MDS algorithm used for the final embedding. One
of \code{c("sgd_dense", "classic")}. Defaults to \code{"sgd_dense"}.
\item graph_symmetry - String. Method used to symmetrise the affinity graph.
One of \code{c("additive", "multiplicative", "mnn", "none")}. Defaults to
\code{"additive"}.
}
}
\description{
Wrapper function to generate PHATE parameters
}
