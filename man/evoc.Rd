% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evoc.R
\name{evoc}
\alias{evoc}
\title{Rust-based EVoC clustering}
\usage{
evoc(
  data,
  knn = NULL,
  n_neighbours = 15L,
  knn_method = c("hnsw", "annoy", "nndescent", "balltree", "ivf", "exhaustive"),
  nn_params = params_nn(),
  evoc_params = params_evoc(),
  return_knn = FALSE,
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to cluster, of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, EVoC will skip
the kNN graph generation and use this one. Defaults to \code{NULL}.}

\item{n_neighbours}{Integer. Number of nearest neighbours for graph
construction. Defaults to \code{15L}.}

\item{knn_method}{Character. Approximate nearest neighbour algorithm to use.
One of \code{"hnsw"}, \code{"annoy"}, \code{"nndescent"}, \code{"balltree"}, \code{"ivf"}, or
\code{"exhaustive"}. Defaults to \code{"hnsw"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn]{params_nn()}}.}

\item{evoc_params}{Named list. EVoC algorithm parameters, see
\code{\link[=params_evoc]{params_evoc()}}.}

\item{return_knn}{Logical. Whether to return the kNN graph. If \code{knn} is
provided, it is returned as-is. Defaults to \code{FALSE}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
An \code{Evoc} S3 object (see \link{print.Evoc}, \link{best_membership},
\link{get_layer}).
}
\description{
Performs EVoC (Embedding Vector Oriented Clustering) on the
input data. Combines a UMAP-like node embedding with HDBSCAN-style
density-based clustering and multi-layer persistence analysis.
}
