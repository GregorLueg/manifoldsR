% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_fa2}
\alias{params_fa2}
\title{Wrapper function to generate ForceAtlas2 parameters}
\usage{
params_fa2(
  local_connectivity = 1,
  bandwidth = 1e-05,
  n_epochs = 500L,
  scaling_ratio = 2,
  gravity = 1,
  strong_gravity = FALSE,
  lin_log = FALSE,
  dissuade_hubs = FALSE,
  edge_weight_influence = 1,
  jitter_tolerance = 1,
  theta = 1.2,
  init = c("spectral", "pca", "random"),
  randomised = FALSE
)
}
\arguments{
\item{local_connectivity}{Numeric. Number of nearest neighbours assumed to be
at distance zero. Defaults to \code{1.0}.}

\item{bandwidth}{Numeric. Convergence tolerance for smooth kNN distance
binary search. Defaults to \code{1e-05}.}

\item{n_epochs}{Integer. Number of optimisation epochs. Defaults to \code{500L}.}

\item{scaling_ratio}{Numeric. Repulsion strength. Larger values spread the
layout. Defaults to \code{2.0}.}

\item{gravity}{Numeric. Pull towards the origin. Keeps disconnected
components from drifting off. Defaults to \code{1.0}.}

\item{strong_gravity}{Boolean. Distance-independent gravity, scaled by
\code{scaling_ratio}. Defaults to \code{FALSE}.}

\item{lin_log}{Boolean. Logarithmic attraction (LinLog). Gives tighter
communities. Defaults to \code{FALSE}.}

\item{dissuade_hubs}{Boolean. Divide the attraction by the node mass. Pushes
hubs to the periphery. Defaults to \code{FALSE}.}

\item{edge_weight_influence}{Numeric. Exponent applied to the edge weights.
\code{0} ignores the weights. Defaults to \code{1.0}.}

\item{jitter_tolerance}{Numeric. Tolerated swinging. Larger is faster but
less precise. Defaults to \code{1.0}.}

\item{theta}{Numeric. Barnes-Hut opening parameter on Gephi's scale. \code{0}
gives the exact repulsion. Defaults to \code{1.2}.}

\item{init}{String. Embedding initialisation method. One of \code{c("spectral", "pca", "random")}. Defaults to \code{"spectral"}.}

\item{randomised}{Boolean. Use randomised SVD for PCA initialisation.
Defaults to \code{FALSE}.}
}
\value{
A named list with the following elements:
\itemize{
\item local_connectivity - Numeric. Number of nearest neighbours assumed to
be at distance zero. Defaults to \code{1.0}.
\item bandwidth - Numeric. Convergence tolerance for smooth kNN distance
binary search. Defaults to \code{1e-05}.
\item n_epochs - Integer. Number of optimisation epochs. Defaults to \code{500L}.
\item scaling_ratio - Numeric. Repulsion strength. Larger values spread the
layout. Defaults to \code{2.0}.
\item gravity - Numeric. Pull towards the origin. Keeps disconnected
components from drifting off. Defaults to \code{1.0}.
\item strong_gravity - Boolean. Distance-independent gravity, scaled by
\code{scaling_ratio}. Defaults to \code{FALSE}.
\item lin_log - Boolean. Logarithmic attraction (LinLog). Gives tighter
communities. Defaults to \code{FALSE}.
\item dissuade_hubs - Boolean. Divide the attraction by the node mass.
Pushes hubs to the periphery. Defaults to \code{FALSE}.
\item edge_weight_influence - Numeric. Exponent applied to the edge weights.
\code{0} ignores the weights. Defaults to \code{1.0}.
\item jitter_tolerance - Numeric. Tolerated swinging. Larger is faster but
less precise. Defaults to \code{1.0}.
\item theta - Numeric. Barnes-Hut opening parameter on Gephi's scale. \code{0}
gives the exact repulsion. Defaults to \code{1.2}.
\item init - String. Embedding initialisation method. One of \code{c("spectral", "pca", "random")}. Defaults to \code{"spectral"}.
\item randomised - Boolean. Use randomised SVD for PCA initialisation.
Defaults to \code{FALSE}.
}
}
\description{
The graph, initialisation and optimisation knobs for
ForceAtlas2. \code{local_connectivity}, \code{bandwidth}, \code{init} and \code{randomised} only
apply when the graph is built from the data via \code{\link[=forceatlas2]{forceatlas2()}};
\code{\link[=forceatlas2_from_graph]{forceatlas2_from_graph()}} ignores them.
}
\references{
Jacomy, et al., PLoS ONE, 2014
}
