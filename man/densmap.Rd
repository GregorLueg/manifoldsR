% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/densmap.R
\name{densmap}
\alias{densmap}
\title{Rust-based densMAP}
\usage{
densmap(
  data,
  knn = NULL,
  n_dim = 2L,
  k = 15L,
  min_dist = 0.5,
  spread = 1,
  knn_method = c("kmknn", "balltree", "hnsw", "annoy", "nndescent", "exhaustive", "ivf"),
  nn_params = params_nn(),
  umap_params = params_umap(),
  dens_params = params_densmap(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to embed of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, densMAP will
skip the k-nearest neighbour graph generation and use this one. Defaults to
\code{NULL}.}

\item{n_dim}{Integer. Number of dimensions in the embedding space.
Defaults to \code{2L}.}

\item{k}{Integer. Number of nearest neighbours to consider for manifold
approximation. Larger values result in more global structure being
preserved. Defaults to \code{15L}.}

\item{min_dist}{Numeric. Minimum distance between points in the embedding.
Controls how tightly points are packed. Smaller values result in more
clustered embeddings. Must be >= 0. Defaults to \code{0.5}. If you use SGD,
consider reducing this!}

\item{spread}{Numeric. Effective scale of embedded points. Determines the
scale at which embedded points will be spread out. Defaults to \code{1.0}.}

\item{knn_method}{Character. (Approximate) Nearest neighbour method to use.
One of \code{"kmknn"}, \code{"hnsw"}, \code{"annoy"}, \code{"nndescent"}, \code{"balltree"}, \code{"ivf"}
or \code{"exhaustive"}. Defaults to \code{"kmknn"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn]{params_nn()}}.}

\item{umap_params}{Named list. UMAP algorithm parameters, see
\code{\link[=params_umap]{params_umap()}}.}

\item{dens_params}{Named list. Density-preservation parameters, see
\code{\link[=params_densmap]{params_densmap()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{use_high_precision}{Optional boolean. Gives fine-grained control over
\code{fp32} vs \code{fp64} usage.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions samples x n_dim containing
the densMAP embedding.
}
\description{
Performs densMAP dimensionality reduction on the input data.
densMAP is UMAP with an added density-preserving term, so a tight cluster
stays tight and a diffuse one stays diffuse. Plain UMAP gives you no such
guarantee: relative sizes in the embedding mean nothing. This function
provides a user-friendly interface with input validation before calling the
Rust implementation.
}
\details{
Setting \code{lambda} to \code{0} in \code{\link[=params_densmap]{params_densmap()}} recovers plain
\code{\link[=umap]{umap()}} exactly. The density term is only active over the final \code{frac} of
the epochs, which is why it costs comparatively little.
}
\references{
Narayan, Berger & Cho, Nat. Biotechnol., 2021
}
