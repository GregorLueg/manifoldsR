% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{params_nn}
\alias{params_nn}
\title{Wrapper function to generate nearest neighbour parameters}
\usage{
params_nn(
  dist_metric = c("euclidean", "cosine"),
  n_tree = 50L,
  search_budget = NULL,
  m = 16L,
  ef_construction = 100L,
  ef_search = 100L,
  diversify_prob = 0,
  delta = 0.001,
  ef_budget = NULL,
  bt_budget = 0.1,
  n_list = NULL,
  n_probes = NULL
)
}
\arguments{
\item{dist_metric}{Character. The distance metric to use. Defaults to
\code{"cosine"}.}

\item{n_tree}{Integer. Number of trees for Annoy. Defaults to \code{50L}.}

\item{search_budget}{Integer or \code{NULL}. Search budget for Annoy. Defaults
to \code{NULL}.}

\item{m}{Integer. Number of bidirectional links for HNSW. Defaults to \code{16L}.}

\item{ef_construction}{Integer. Size of the dynamic candidate list during
HNSW construction. Defaults to \code{100L}.}

\item{ef_search}{Integer. Size of the dynamic candidate list during HNSW
search. Defaults to \code{100L}.}

\item{diversify_prob}{Float. Diversification probability for NN descent.
Defaults to \code{0.0}.}

\item{delta}{Float. Precision parameter for NN descent. Defaults to \code{0.001}.}

\item{ef_budget}{Integer or \code{NULL}. Effort budget for NN descent. Defaults
to \code{NULL}.}

\item{bt_budget}{Float. Budget for ball tree search. Defaults to \code{0.1}.}

\item{n_list}{Optional integer. Number of clusters to use for IVF. If \code{NULL},
will default to \code{sqrt(n)}.}

\item{n_probes}{Optional integer. Number of clusters to probe for IVF. If
\code{NULL}, will default to \code{sqrt(n_list)}.}
}
\value{
A list with the nearest neighbour parameters.
}
\description{
Wrapper function to generate nearest neighbour parameters
}
