% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic-data-gen.R
\name{rs_synthetic_data}
\alias{rs_synthetic_data}
\title{Generate synthetic data for manifold learning}
\usage{
rs_synthetic_data(
  type = c("swiss_role", "clusters", "tree"),
  n_samples,
  dim = 2L,
  n_clusters = 3L,
  n_branches = 3L,
  noise = 0.1,
  seed = 42L
)
}
\arguments{
\item{type}{Character. Type of synthetic data to generate. One of:
\itemize{
\item "swiss_role" - Swiss role manifold
\item "clusters" - Clustered data
\item "tree" - Tree-like/branching data
}}

\item{n_samples}{Integer. Number of data points to generate.}

\item{dim}{Integer. Dimensionality of the data (used for "clusters" and "tree").}

\item{n_clusters}{Integer. Number of clusters (used for "clusters" type). Default is 3.}

\item{n_branches}{Integer. Number of branches (used for "tree" type). Default is 3.}

\item{noise}{Numeric. Amount of noise to add (used for "swiss_role" and "tree"). must be any non 0 positive value. Default is 0.1.}

\item{seed}{Integer. Random seed for reproducibility. Default is NULL (no seed).}
}
\value{
A list with the following elements:
\itemize{
\item data - Numerical matrix with the generated data
\item membership - Vector of cluster/branch assignments (NULL for swiss_role)
}
}
\description{
A unified wrapper function for generating various types of
synthetic data to test manifold learning techniques.
}
\examples{
\dontrun{
# Generate Swiss role data
swiss <- rs_synthetic_data("swiss_role", n_samples = 1000, noise = 0.1, seed = 42)

# Generate clustered data
clusters <- rs_synthetic_data("clusters", n_samples = 500, dim = 10,
                              n_clusters = 5, seed = 42)

# Generate tree-like data
tree <- rs_synthetic_data("tree", n_samples = 800, dim = 10,
                          n_branches = 4, noise = 0.05, seed = 42)
}

}
