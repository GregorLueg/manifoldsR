% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{params_densne}
\alias{params_densne}
\title{Wrapper function to generate den-SNE parameters}
\usage{
params_densne(lambda = 0.1, frac = 0.3, var_shift = 0.1)
}
\arguments{
\item{lambda}{Numeric. Weight of the density term. \code{0} disables it. Defaults
to \code{0.1}, the den-SNE reference value.}

\item{frac}{Numeric between 0 and 1. Fraction of the total epochs, at the
end of the run, over which the density term is active. Defaults to \code{0.3}.}

\item{var_shift}{Numeric. Additive shift on the variance of the embedding
log-radii. Defaults to \code{0.1}.}
}
\value{
A list with the density-preservation parameters.
}
\description{
The density-preservation knobs on top of the usual t-SNE parameters. den-SNE
adds \verb{-lambda * Corr(log Ro, log Re)} to the t-SNE loss, where \code{Ro} is the
local radius in the input space and \code{Re} the matching radius in the
embedding. Setting \code{lambda} to \code{0} recovers plain t-SNE. The default weight
is twenty times smaller than the densMAP one, matching the reference
implementations.
}
\references{
Narayan, Berger & Cho, Nat. Biotechnol., 2021
}
