% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_densne}
\alias{params_densne}
\title{Wrapper function to generate den-SNE parameters}
\usage{
params_densne(lambda = 0.5, frac = 0.3, var_shift = 0.1)
}
\arguments{
\item{lambda}{Numeric. Weight of the density term. \code{0} disables it. The
default is higher than the den-SNE reference value (original: \code{0.1}).
Defaults to \code{0.5}.}

\item{frac}{Numeric. Fraction of the total epochs, at the end of the run,
over which the density term is active. Defaults to \code{0.3}.}

\item{var_shift}{Numeric. Additive shift on the variance of the embedding
log-radii. Defaults to \code{0.1}.}
}
\value{
A named list with the following elements:
\itemize{
\item lambda - Numeric. Weight of the density term. \code{0} disables it. The
default is higher than the den-SNE reference value (original: \code{0.1}).
Defaults to \code{0.5}.
\item frac - Numeric. Fraction of the total epochs, at the end of the run,
over which the density term is active. Defaults to \code{0.3}.
\item var_shift - Numeric. Additive shift on the variance of the embedding
log-radii. Defaults to \code{0.1}.
}
}
\description{
The density-preservation knobs on top of the usual t-SNE
parameters. den-SNE adds \verb{-lambda * Corr(log Ro, log Re)} to the t-SNE loss,
where \code{Ro} is the local radius in the input space and \code{Re} the matching
radius in the embedding. Setting \code{lambda} to \code{0} recovers plain t-SNE.
}
\references{
Narayan, Berger & Cho, Nat. Biotechnol., 2021
}
