% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/densne.R
\name{densne}
\alias{densne}
\title{Rust-based den-SNE}
\usage{
densne(
  data,
  knn = NULL,
  n_dim = 2L,
  perplexity = 20,
  approx_type = c("bh", "fft"),
  knn_method = c("kmknn", "balltree", "hnsw", "annoy", "nndescent", "exhaustive", "ivf"),
  nn_params = params_nn(),
  tsne_params = params_tsne(),
  dens_params = params_densne(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to embed of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, den-SNE will skip
the k-nearest neighbour graph generation and use this one. Defaults to
\code{NULL}.}

\item{n_dim}{Integer. Number of dimensions in the embedding space.
Currently only \code{2L} is supported. Defaults to \code{2L}.}

\item{perplexity}{Numeric. Perplexity parameter, related to the number of
nearest neighbours used in manifold learning. Typical values are between
5 and 50. Defaults to \code{20.0}.}

\item{approx_type}{Character. Approximation method for computing repulsive
forces. One of \code{"bh"} for Barnes-Hut or \code{"fft"} for FFT-accelerated
interpolation. Defaults to \code{"bh"}.}

\item{knn_method}{Character. (Approximate) Nearest neighbour method to use.
One of \code{"kmknn"}, \code{"hnsw"}, \code{"annoy"}, \code{"nndescent"}, \code{"balltree"}, \code{"ivf"}
or \code{"exhaustive"}. Defaults to \code{"kmknn"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn]{params_nn()}}.}

\item{tsne_params}{Named list. t-SNE algorithm parameters, see
\code{\link[=params_tsne]{params_tsne()}}.}

\item{dens_params}{Named list. Density-preservation parameters, see
\code{\link[=params_densne]{params_densne()}}.}

\item{seed}{Integer. Random seed for reproducibility.}

\item{use_high_precision}{Optional boolean. Gives fine-grained control over
\code{fp32} vs \code{fp64} usage.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions samples x n_dim containing
the den-SNE embedding.
}
\description{
Performs den-SNE dimensionality reduction on the input data.
den-SNE is t-SNE with an added density-preserving term, so a tight cluster
stays tight and a diffuse one stays diffuse. Plain t-SNE gives you no such
guarantee: relative sizes in the embedding mean nothing. This function
provides a user-friendly interface with input validation before calling the
Rust implementation.
}
\details{
The number of neighbours will be \code{3 * perplexity}, as this is a
usual default in tSNE. Setting \code{lambda} to \code{0} in \code{\link[=params_densne]{params_densne()}} recovers
plain \code{\link[=tsne]{tsne()}} exactly. The default \code{lambda} is twenty times smaller than the
densMAP one, matching the reference implementations.
}
\references{
Narayan, Berger & Cho, Nat. Biotechnol., 2021
}
