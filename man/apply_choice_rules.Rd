% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-prelude-generated.R
\name{apply_choice_rules}
\alias{apply_choice_rules}
\title{Apply testChoice rules by name}
\usage{
apply_choice_rules(x, rules, label, hint = NULL)
}
\arguments{
\item{x}{Named list of parameters to validate.}

\item{rules}{Named list mapping field name to the character vector of
allowed choices.}

\item{label}{Short human-readable label used in the error message
(e.g. \code{"MELD params"}).}

\item{hint}{Optional string appended to the error message. Defaults to
\code{NULL} (no hint).}
}
\value{
\code{TRUE} if all checked fields pass, otherwise a string naming the
first offending element.
}
\description{
Validates the elements of a named list \code{x} against per-field
\code{\link[checkmate:testChoice]{checkmate::testChoice()}} choice sets. Fields whose names are not in \code{rules}
are skipped. On failure, returns an error string naming the first offending
element and appending an optional \code{hint}.
}
\keyword{internal}
