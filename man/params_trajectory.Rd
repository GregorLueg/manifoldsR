% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{params_trajectory}
\alias{params_trajectory}
\title{Parameters for trajectory data generation}
\usage{
params_trajectory(
  topology = c("bifurcation", "linear", "combination"),
  cell_trajectories = NULL,
  noise = 0.1
)
}
\arguments{
\item{topology}{Character. One of \code{c("bifurcation", "linear", "combination")}. Ignored if \code{cell_trajectories} is not \code{NULL}. Defaults to
\code{"bifurcation"}.}

\item{cell_trajectories}{Optional list. Named list with three equal-length
vectors: \code{parent} (integer, \code{NA} for root, zero-indexed), \code{split_at}
(numeric, fraction along parent where branch starts), and \code{length} (numeric,
length of the branch). If \code{NULL}, \code{topology} is used instead. Defaults to
\code{NULL}.}

\item{noise}{Numeric. Amount of noise to add. Must be a positive non-zero
value. Defaults to \code{0.1}.}
}
\value{
A list of parameters for use with \code{\link[=manifold_synthetic_data]{manifold_synthetic_data()}}.
}
\description{
Parameters for trajectory data generation
}
