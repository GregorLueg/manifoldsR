% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{params_evoc}
\alias{params_evoc}
\title{Wrapper function to generate EVoC parameters}
\usage{
params_evoc(
  noise_level = 0.5,
  n_epochs = 50L,
  embedding_dim = NULL,
  neighbour_scale = 1,
  symmetrise = TRUE,
  min_samples = 5L,
  base_min_cluster_size = 5L,
  approx_n_clusters = NULL,
  min_similarity_threshold = 0.2,
  max_layers = 10L
)
}
\arguments{
\item{noise_level}{Numeric. Noise level for the embedding gradient.
\code{0.0} = aggressive, \code{1.0} = conservative. Defaults to \code{0.5}.}

\item{n_epochs}{Integer. Number of embedding optimisation epochs. Defaults
to \code{50L}.}

\item{embedding_dim}{Integer or \code{NULL}. Embedding dimensionality. If \code{NULL},
defaults to \code{min(max(n_neighbours / 4, 4), 16)}. Defaults to \code{NULL}.}

\item{neighbour_scale}{Numeric. Multiplier on effective neighbours for
fuzzy graph construction. Defaults to \code{1.0}.}

\item{symmetrise}{Logical. Whether to symmetrise the fuzzy graph. Defaults
to \code{TRUE}.}

\item{min_samples}{Integer. Minimum samples for core distance in MST
density estimation. Defaults to \code{5L}.}

\item{base_min_cluster_size}{Integer. Base minimum cluster size for the
finest layer. Defaults to \code{5L}.}

\item{approx_n_clusters}{Integer or \code{NULL}. If set, binary-searches for
approximately this many clusters (single layer output). Defaults to \code{NULL}.}

\item{min_similarity_threshold}{Numeric. Jaccard similarity threshold for
filtering redundant layers. Defaults to \code{0.2}.}

\item{max_layers}{Integer. Maximum number of cluster layers to return.
Defaults to \code{10L}.}
}
\value{
A list with the EVoC parameters.
}
\description{
Wrapper function to generate EVoC parameters
}
