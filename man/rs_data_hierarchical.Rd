% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{rs_data_hierarchical}
\alias{rs_data_hierarchical}
\title{Generate hierarchical cluster data}
\usage{
rs_data_hierarchical(
  n_samples,
  dim,
  n_supergroups,
  n_subclusts,
  supergroup_spread,
  subcluster_spread,
  point_std,
  seed
)
}
\arguments{
\item{n_samples}{Integer. Total number of points, distributed evenly across
all subclusters.}

\item{dim}{Integer. Dimensionality of the ambient space.}

\item{n_supergroups}{Integer. Number of top-level groups. Defaults to \code{3}.}

\item{n_subclusts}{Integer. Number of subclusters per supergroup. Defaults
to \code{3}.}

\item{supergroup_spread}{Numeric. Spread of supergroup centres. Defaults to
\code{15.0}.}

\item{subcluster_spread}{Numeric. Spread of subcluster centres around their
supergroup centre. Defaults to \code{2.0}.}

\item{point_std}{Numeric. Within-subcluster Gaussian noise. Defaults to
\code{0.4}.}

\item{seed}{Integer. Seed for reproducibility.}
}
\value{
A named list with three elements: \code{data}, a numeric matrix of shape
samples x \code{dim}; \code{supergroup}, an integer vector of supergroup labels
(\verb{0..n_supergroups}) one per sample; and \code{subgroup}, an integer vector of
subcluster labels (\verb{0..n_supergroups * n_subclusts}) one per sample.
}
\description{
Generates synthetic data with a two-level cluster hierarchy:
\code{n_supergroups} top-level groups each containing \code{n_subclusts} tight
subclusters. Supergroup centres are spread far apart; subcluster centres sit
tightly around their supergroup centre.

Note that the actual number of samples returned may be slightly less than
\code{n_samples} if it is not evenly divisible by \code{n_supergroups * n_subclusts}.
}
