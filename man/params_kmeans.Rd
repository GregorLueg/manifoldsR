% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params.R
\name{params_kmeans}
\alias{params_kmeans}
\title{Wrapper function to generate k-means parameters}
\usage{
params_kmeans(
  metric = c("euclidean", "cosine"),
  max_iters = 1000L,
  batch_size = 4096L,
  drift_threshold = 1e-04,
  lr_alpha = 1,
  init = c("parallel", "random"),
  use_hamerly = NULL,
  use_gemm = NULL
)
}
\arguments{
\item{metric}{Character. Distance metric to use. One of \code{"euclidean"} or
\code{"cosine"}. Defaults to \code{"euclidean"}.}

\item{max_iters}{Integer. Maximum number of iterations. Defaults to \code{1000L}.}

\item{batch_size}{Integer. Mini-batch size. Only used when
\code{method = "minibatch"}. Defaults to \code{4096L}.}

\item{drift_threshold}{Float. Below which centroid drift the mini-batch
k-means is considered converged. Defaults to \code{1e-4}. Only used when
\code{method = "minibatch"}}

\item{lr_alpha}{Float. Learning rate decay for the mini-batch k-means.
Original paper uses \code{1.0}.}

\item{init}{String. One of \code{c("parallel", "random")}. The initialisation of
the centroids.}

\item{use_hamerly}{Optional boolean. Shall Hamerly's method be used (only
available if \code{metric == "euclidean"}).}

\item{use_gemm}{Optional boolean. Shall the GEMM path be used. Useful on
high dimensional data. If \code{NULL}, choice will be based on heuristics.}
}
\value{
A named list with the k-means parameters.
}
\description{
Wrapper function to generate k-means parameters
}
