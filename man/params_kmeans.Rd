% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_kmeans}
\alias{params_kmeans}
\title{Wrapper function to generate k-means parameters}
\usage{
params_kmeans(
  metric = c("euclidean", "cosine"),
  max_iters = 1000L,
  batch_size = 4096L,
  drift_threshold = 1e-04,
  lr_alpha = 1,
  init = c("parallel", "random"),
  use_hamerly = NULL,
  use_gemm = NULL
)
}
\arguments{
\item{metric}{String. Distance metric to use. One of \code{c("euclidean", "cosine")}. Defaults to \code{"euclidean"}.}

\item{max_iters}{Integer. Maximum number of iterations. Defaults to \code{1000L}.}

\item{batch_size}{Integer. Mini-batch size. Only used when \code{method = "minibatch"}. Defaults to \code{4096L}.}

\item{drift_threshold}{Numeric. Below which centroid drift the mini-batch
k-means is considered converged. Only used when \code{method = "minibatch"}.
Defaults to \code{1e-04}.}

\item{lr_alpha}{Numeric. Learning rate decay for the mini-batch k-means.
Original paper uses \code{1.0}. Defaults to \code{1.0}.}

\item{init}{String. The initialisation of the centroids. One of
\code{c("parallel", "random")}. Defaults to \code{"parallel"}.}

\item{use_hamerly}{Boolean or \code{NULL}. Shall Hamerly's method be used (only if
\code{metric == "euclidean"}). Defaults to \code{NULL}.}

\item{use_gemm}{Boolean or \code{NULL}. Shall the GEMM path be used. Useful on
high dimensional data. If \code{NULL}, choice will be based on heuristics.
Defaults to \code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item metric - String. Distance metric to use. One of \code{c("euclidean", "cosine")}. Defaults to \code{"euclidean"}.
\item max_iters - Integer. Maximum number of iterations. Defaults to
\code{1000L}.
\item batch_size - Integer. Mini-batch size. Only used when \code{method = "minibatch"}. Defaults to \code{4096L}.
\item drift_threshold - Numeric. Below which centroid drift the mini-batch
k-means is considered converged. Only used when \code{method = "minibatch"}.
Defaults to \code{1e-04}.
\item lr_alpha - Numeric. Learning rate decay for the mini-batch k-means.
Original paper uses \code{1.0}. Defaults to \code{1.0}.
\item init - String. The initialisation of the centroids. One of
\code{c("parallel", "random")}. Defaults to \code{"parallel"}.
\item use_hamerly - Boolean or \code{NULL}. Shall Hamerly's method be used (only
if \code{metric == "euclidean"}). Defaults to \code{NULL}.
\item use_gemm - Boolean or \code{NULL}. Shall the GEMM path be used. Useful on
high dimensional data. If \code{NULL}, choice will be based on heuristics.
Defaults to \code{NULL}.
}
}
\description{
Wrapper function to generate k-means parameters
}
