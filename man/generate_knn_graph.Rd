% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nearest_neighbours.R
\name{generate_knn_graph}
\alias{generate_knn_graph}
\title{Generate a k-nearest neighbour graph.}
\usage{
generate_knn_graph(
  data,
  k,
  knn_method = c("hnsw", "annoy", "nndescent", "balltree", "exhaustive"),
  nn_params = params_nn(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numeric matrix. The embedding or feature matrix to compute
neighbours on. Rows are observations, columns are features.}

\item{k}{Integer. The number of nearest neighbours to compute.}

\item{knn_method}{Character. The algorithm to use for nearest neighbour
search. One of \code{c("hnsw", "annoy", "nndescent", "balltree", "exhaustive")}.
Defaults to \code{"hnsw"}.}

\item{nn_params}{List. Output of \code{\link[=params_nn]{params_nn()}}. Controls
algorithm-specific parameters.}

\item{seed}{Integer. For reproducibility. Defaults to \code{42L}.}

\item{.verbose}{Boolean. Controls verbosity.}
}
\value{
A nearest neighbours class object with 1-indexed neighbour indices
and distances.
}
\description{
This function generates a kNN graph based on a given numeric matrix. Four
different algorithms are implemented with different speed and accuracy
trade-offs:
\itemize{
\item \code{"hnsw"} - Hierarchical Navigable Small Worlds vector search with
slower index generation (which ammortises on larger data sets) and high
precision.
\item \code{"annoy"} - Approximate Nearest Neighbours Oh Yeah algorithm, faster
to index but querying on large data sets can be slow.
\item \code{"nndescent"} - Rust-based implementation of the PyNNDescent
algorithm, a good all-rounder that performs well on very large data sets.
\item \code{"balltree"} - Ball tree structure for exact search with a
configurable budget.
\item \code{"exhaustive"} - Exact nearest neighbour search.
}
}
