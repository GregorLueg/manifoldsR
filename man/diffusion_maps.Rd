% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diffusion_maps.R
\name{diffusion_maps}
\alias{diffusion_maps}
\title{Rust-based diffusion maps}
\usage{
diffusion_maps(
  data,
  knn = NULL,
  n_dim = 2L,
  k = 5L,
  knn_method = c("kmknn", "balltree", "hnsw", "annoy", "nndescent", "exhaustive"),
  nn_params = params_nn(),
  dm_params = params_diffusion_maps(),
  seed = 42L,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to embed of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, diffusion maps
will skip the k-nearest neighbour graph generation and use this one.
Defaults to \code{NULL}.}

\item{n_dim}{Integer. Number of dimensions in the embedding space.
Defaults to \code{2L}.}

\item{k}{Integer. Number of nearest neighbours to consider for the
affinity graph. Defaults to \code{5L}.}

\item{knn_method}{Character. (Approximate) Nearest neighbour method to use.
One of \code{"kmknn"}, \code{"hnsw"}, \code{"annoy"}, \code{"nndescent"}, \code{"balltree"}, \code{"ivf"}
or \code{"exhaustive"}. Defaults to \code{"kmknn"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn]{params_nn()}}.}

\item{dm_params}{Named list. Diffusion maps algorithm parameters, see
\code{\link[=params_diffusion_maps]{params_diffusion_maps()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions samples x n_dim containing the
diffusion maps embedding.
}
\description{
Performs diffusion maps dimensionality reduction on the input
data. This function provides a user-friendly interface with input
validation before calling the Rust implementation.
}
