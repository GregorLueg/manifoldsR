% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_diffusion_maps}
\alias{params_diffusion_maps}
\title{Wrapper function to generate diffusion maps parameters}
\usage{
params_diffusion_maps(
  bandwidth_scale = 1,
  thresh = 1e-04,
  graph_symmetry = c("additive", "multiplicative", "mnn", "none"),
  alpha_norm = 1,
  t_max = 100L,
  t_custom = NULL,
  n_landmarks = 2048L,
  landmark_method = c("spectral", "random", "density"),
  n_svd = NULL
)
}
\arguments{
\item{bandwidth_scale}{Numeric. Multiplicative factor applied to the adaptive
kernel bandwidth. Defaults to \code{1.0}.}

\item{thresh}{Numeric. Sparsity threshold applied to kernel entries. Defaults
to \code{1e-04}.}

\item{graph_symmetry}{String. Method used to symmetrise the affinity graph.
One of \code{c("additive", "multiplicative", "mnn", "none")}. Defaults to
\code{"additive"}.}

\item{alpha_norm}{Numeric. Anisotropic density-correction exponent. \code{0} gives
the normalised graph Laplacian, \code{0.5} the Fokker-Planck operator, \code{1} the
Laplace-Beltrami operator. Defaults to \code{1.0}.}

\item{t_max}{Integer. Maximum diffusion time considered during automatic
selection via Von Neumann entropy knee point detection. Defaults to \code{100L}.}

\item{t_custom}{Integer or \code{NULL}. Fixed diffusion time. If set, overrides
automatic time selection. Defaults to \code{NULL}.}

\item{n_landmarks}{Integer or \code{NULL}. Number of landmarks for compressed
diffusion. If \code{NULL}, the full N x N diffusion operator is used. Defaults to
\code{2048L}.}

\item{landmark_method}{String. Method used to select landmarks. One of
\code{c("spectral", "random", "density")}. Defaults to \code{"spectral"}.}

\item{n_svd}{Integer or \code{NULL}. Number of SVD components used in landmark
construction. If \code{NULL}, the library selects a sensible default. Defaults to
\code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item bandwidth_scale - Numeric. Multiplicative factor applied to the
adaptive kernel bandwidth. Defaults to \code{1.0}.
\item thresh - Numeric. Sparsity threshold applied to kernel entries.
Defaults to \code{1e-04}.
\item graph_symmetry - String. Method used to symmetrise the affinity graph.
One of \code{c("additive", "multiplicative", "mnn", "none")}. Defaults to
\code{"additive"}.
\item alpha_norm - Numeric. Anisotropic density-correction exponent. \code{0}
gives the normalised graph Laplacian, \code{0.5} the Fokker-Planck operator, \code{1}
the Laplace-Beltrami operator. Defaults to \code{1.0}.
\item t_max - Integer. Maximum diffusion time considered during automatic
selection via Von Neumann entropy knee point detection. Defaults to \code{100L}.
\item t_custom - Integer or \code{NULL}. Fixed diffusion time. If set, overrides
automatic time selection. Defaults to \code{NULL}.
\item n_landmarks - Integer or \code{NULL}. Number of landmarks for compressed
diffusion. If \code{NULL}, the full N x N diffusion operator is used. Defaults to
\code{2048L}.
\item landmark_method - String. Method used to select landmarks. One of
\code{c("spectral", "random", "density")}. Defaults to \code{"spectral"}.
\item n_svd - Integer or \code{NULL}. Number of SVD components used in landmark
construction. If \code{NULL}, the library selects a sensible default. Defaults to
\code{NULL}.
}
}
\description{
Wrapper function to generate diffusion maps parameters
}
