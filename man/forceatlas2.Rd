% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fa2.R
\name{forceatlas2}
\alias{forceatlas2}
\title{Rust-based ForceAtlas2}
\usage{
forceatlas2(
  data,
  knn = NULL,
  k = 15L,
  knn_method = c("kmknn", "balltree", "hnsw", "annoy", "nndescent", "exhaustive", "ivf"),
  nn_params = params_nn(),
  fa2_params = params_fa2(),
  seed = 42L,
  use_high_precision = NULL,
  .verbose = TRUE
)
}
\arguments{
\item{data}{Numerical matrix or data frame. The data to embed of shape
samples x features. Will be coerced to a matrix.}

\item{knn}{Optional \code{NearestNeighbours} class. If provided, the k-nearest
neighbour search is skipped and this one is used. Defaults to \code{NULL}.}

\item{k}{Integer. Number of nearest neighbours. Defaults to \code{15L}.}

\item{knn_method}{Character. (Approximate) Nearest neighbour method to use.
One of \code{"kmknn"}, \code{"hnsw"}, \code{"annoy"}, \code{"nndescent"}, \code{"balltree"}, \code{"ivf"}
or \code{"exhaustive"}. Defaults to \code{"kmknn"}.}

\item{nn_params}{Named list. Nearest neighbour search parameters, see
\code{\link[=params_nn]{params_nn()}}.}

\item{fa2_params}{Named list. ForceAtlas2 parameters, see \code{\link[=params_fa2]{params_fa2()}}.}

\item{seed}{Integer. Random seed for reproducibility. Defaults to \code{42L}.}

\item{use_high_precision}{Optional boolean. Gives fine-grained control over
\code{fp32} vs \code{fp64} usage.}

\item{.verbose}{Logical. Controls verbosity. Defaults to \code{TRUE}.}
}
\value{
A numerical matrix with dimensions samples x 2 containing the
ForceAtlas2 layout.
}
\description{
Lays out the kNN graph of the data with ForceAtlas2, the way
scanpy's \code{draw_graph} does: the kNN graph is turned into the UMAP fuzzy
union graph and every edge pulls, every node pair pushes (\code{1/d}, with masses
\code{1 + degree}), plus gravity towards the origin. Repulsion runs on a
Barnes-Hut tree. Already have a graph? Use \code{\link[=forceatlas2_from_graph]{forceatlas2_from_graph()}}.
}
\references{
Jacomy, et al., PLoS ONE, 2014
}
