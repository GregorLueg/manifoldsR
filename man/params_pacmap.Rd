% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-generated.R
\name{params_pacmap}
\alias{params_pacmap}
\title{Wrapper function to generate PaCMAP parameters}
\usage{
params_pacmap(
  n_near = 10L,
  n_mid_near = 5L,
  n_further = 20L,
  mn_candidate_start = 4L,
  mn_candidate_end = 50L,
  init = c("pca", "random"),
  optimiser = c("adam_parallel", "adam"),
  lr = 1,
  n_epochs = NULL,
  beta1 = 0.9,
  beta2 = 0.999,
  eps = 1e-07,
  phase1_end = NULL,
  phase2_end = NULL
)
}
\arguments{
\item{n_near}{Integer. Near pairs per point (attractive). Defaults to \code{10L}.}

\item{n_mid_near}{Integer. Mid-near pairs per point. Defaults to \code{5L}.}

\item{n_further}{Integer. Further (random) pairs per point. Defaults to
\code{20L}.}

\item{mn_candidate_start}{Integer. Start index into kNN list for mid-near
candidate window. Defaults to \code{4L}.}

\item{mn_candidate_end}{Integer. End index into kNN list for mid-near
candidate window. Also determines the kNN search size. Defaults to \code{50L}.}

\item{init}{String. Embedding initialisation. One of \code{c("pca", "random")}.
Defaults to \code{"pca"}.}

\item{optimiser}{String. The optimiser. One of \code{c("adam_parallel", "adam")}.
Defaults to \code{"adam_parallel"}.}

\item{lr}{Numeric. Adam learning rate. Defaults to \code{1.0}.}

\item{n_epochs}{Integer or \code{NULL}. Total optimisation epochs. If \code{NULL},
resolved downstream to \code{450}. Defaults to \code{NULL}.}

\item{beta1}{Numeric. Adam first moment decay. Defaults to \code{0.9}.}

\item{beta2}{Numeric. Adam second moment decay. Defaults to \code{0.999}.}

\item{eps}{Numeric. Adam numerical stability constant. Defaults to \code{1e-07}.}

\item{phase1_end}{Integer or \code{NULL}. Epoch at which phase 1 ends. If \code{NULL},
resolved downstream to \code{100}. Defaults to \code{NULL}.}

\item{phase2_end}{Integer or \code{NULL}. Epoch at which phase 2 ends. If \code{NULL},
resolved downstream to \code{200}. Defaults to \code{NULL}.}
}
\value{
A named list with the following elements:
\itemize{
\item n_near - Integer. Near pairs per point (attractive). Defaults to
\code{10L}.
\item n_mid_near - Integer. Mid-near pairs per point. Defaults to \code{5L}.
\item n_further - Integer. Further (random) pairs per point. Defaults to
\code{20L}.
\item mn_candidate_start - Integer. Start index into kNN list for mid-near
candidate window. Defaults to \code{4L}.
\item mn_candidate_end - Integer. End index into kNN list for mid-near
candidate window. Also determines the kNN search size. Defaults to \code{50L}.
\item init - String. Embedding initialisation. One of \code{c("pca", "random")}.
Defaults to \code{"pca"}.
\item optimiser - String. The optimiser. One of \code{c("adam_parallel", "adam")}. Defaults to \code{"adam_parallel"}.
\item lr - Numeric. Adam learning rate. Defaults to \code{1.0}.
\item n_epochs - Integer or \code{NULL}. Total optimisation epochs. If \code{NULL},
resolved downstream to \code{450}. Defaults to \code{NULL}.
\item beta1 - Numeric. Adam first moment decay. Defaults to \code{0.9}.
\item beta2 - Numeric. Adam second moment decay. Defaults to \code{0.999}.
\item eps - Numeric. Adam numerical stability constant. Defaults to \code{1e-07}.
\item phase1_end - Integer or \code{NULL}. Epoch at which phase 1 ends. If
\code{NULL}, resolved downstream to \code{100}. Defaults to \code{NULL}.
\item phase2_end - Integer or \code{NULL}. Epoch at which phase 2 ends. If
\code{NULL}, resolved downstream to \code{200}. Defaults to \code{NULL}.
}
}
\description{
Wrapper function to generate PaCMAP parameters
}
