% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_constructors.R
\name{params_tsne}
\alias{params_tsne}
\title{Construct tSNE parameters list}
\usage{
params_tsne(knn_method = "hnsw", dist_metric = "euclidean", theta = 0.5)
}
\arguments{
\item{knn_method}{Character. K-nearest neighbors method to use. One of:
\itemize{
\item "hnsw" - Hierarchical Navigable Small World (fast, approximate)
\item "annoy" - Approximate Nearest Neighbors Oh Yeah
}}

\item{dist_metric}{Character. Distance metric to use. One of:
\itemize{
\item "euclidean" - Euclidean distance (recommended for most cases)
\item "cosine" - Cosine distance
\item "manhattan" - Manhattan distance
}}

\item{theta}{Numeric. Theta parameter for Barnes-Hut approximation.
Controls the speed-accuracy tradeoff. Lower values are more accurate but
slower. Typical range is 0.1 to 1.0. Default is 0.5.}
}
\value{
A named list containing the tSNE parameters ready to pass to
\code{rs_tsne}.
}
\description{
Creates a validated named list of parameters to pass to the
\code{tsne_params} argument of \code{rs_tsne}.
}
\examples{
\dontrun{
# Standard parameters for tSNE
params <- params_tsne()

# Customize parameters
params <- params_tsne(
  knn_method = "hnsw",
  dist_metric = "euclidean",
  theta = 0.3
)

# Use with rs_tsne
embedding <- rs_tsne(
  embd = data_matrix,
  n_dim = 2,
  perplexity = 50,
  approx_type = "bh",
  tsne_params = params,  # Use the constructed params
  seed = 42,
  verbose = TRUE
)
}

}
