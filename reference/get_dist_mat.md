# Get the indices as a matrix

Get the indices as a matrix

## Usage

``` r
get_dist_mat(x)

# S3 method for class 'NearestNeighbours'
get_dist_mat(x)
```

## Arguments

- x:

  `NearestNeighbours` class

## Value

The indices in samples x neighbours format
